# 2026-03-22

- User asked for OpenClaw troubleshooting and plugin/model reliability fixes.
- Diagnosed repeated `403 Your request was blocked` + rate-limit/502 as upstream model/provider instability, not plugin warning root cause.
- Confirmed plugin warning causes: duplicate Feishu plugin sources and suspicious ownership from non-stock path.
- Cleaned Feishu plugin config to prefer official stock plugin source; removed duplicate-path configuration patterns.
- Added director reliability tooling and guardrails:
  - New script: `scripts/director-failview.sh` for quick local failure aggregation.
  - Updated `AGENTS.md` with P1 observability + P2 failure-response behavior rules.
  - Commit created in director workspace: `037d8da`.
- Built/fixed model-config-admin plugin artifacts:
  - Reinstalled dependencies with dev packages and rebuilt plugin dist.
  - Built dashboard UI workspace output into plugin `dist/ui` to resolve `UI build not found`.
- Determined version mismatch root cause in user deployment: running gateway from old container app path while CLI showed newer version.
- Guided Docker Compose optimization: pin valid image tags, unify gateway/cli image source, avoid invalid/nonexistent tags, and validate with `docker compose config`.
- User requested Moltbook onboarding:
  - Registered agent name `director_xiaoguaibao` (original Chinese/space name rejected by API constraints).
  - Saved credentials to `/home/node/.config/moltbook/credentials.json` with mode 600.
  - Updated credentials after API key rotation from user.
  - Verified Moltbook status endpoint returns `claimed` and active.
- Security lessons reinforced repeatedly:
  - Do not expose tokens/API keys in chat.
  - Rotate leaked tokens immediately.
  - Use `www.moltbook.com` for auth headers per Moltbook requirement.
- Generated comprehensive OpenClaw config schema exports from live gateway:
  - Full paths TSV: `openclaw-config-fields-full.tsv` (3260 paths).
  - Summary markdown: `openclaw-config-fields-summary.md`.
- New execution standard requested by user: "Delivered isn’t a message: it’s evidence".
  - For future task reports, include a Trust Receipt style summary:
    1) Intent, 2) Action, 3) Evidence, 4) Risk, 5) Reversibility, 6) Next Steps.
  - Prefer artifact-based proof (file paths, diffs, command outputs, commit IDs) over verbal completion claims.

- User preference reinforced: avoid verbal completion claims; use evidence-first delivery with artifact-backed receipts (Intent/Action/Evidence/Risk/Reversibility/Next).
- Moltbook operationalization completed:
  - Created reusable local skill at `workspace/skills/moltbook/` with `SKILL.md` and `scripts/moltbook.mjs`.
  - Credentials persisted at `~/.config/moltbook/credentials.json` (secure perms applied), using rotated key from user.
  - Verified Moltbook account status is `claimed` for agent handle `director_xiaoguaibao`.
- Moltbook engagement behavior requested by user: proactively browse high-quality posts and interact (learn + participate), while avoiding spam/noise threads.
- Concrete interaction proof captured: posted and verified a comment under post `baf4b6e9-e57a-4c4b-8d40-f8131b149479` as `director_xiaoguaibao` (comment id `2e85f41e-d4f4-41cb-a16e-1d7ec48dcfe2`).
- Delivery reliability lesson from this session: when user asks for UI changes, prefer shortest path to verifiable shipped artifact and report exact proof points early.
- Knowledge capture completed: added `knowledge/OPENCLAW_BEST_PRACTICES.md` as a reusable checklist and execution standard for OpenClaw operations.
- Started memory plugin upgrade track with LightRAG focus:
  - Cloned LightRAG source to `workspace/projects/LightRAG`.
  - Verified OpenClaw memory plugin contracts (`kind: \"memory\"`, `plugins.slots.memory`) from docs/source.
  - Created implementation docs under `projects/memory-engine-plugin/` (`ARCHITECTURE.md`, `IMPLEMENTATION_PLAN.md`).
