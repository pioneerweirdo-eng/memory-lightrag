## Durable notes — 2026-03-23 (pre-compaction flush)

- User priority reinforced: for incident/plugin troubleshooting, **report first, then act**; avoid unannounced restarts and avoid rapid trial-and-error changes.
- Explicit user directive: "多调研多思考，不要立马去做" — require research/evidence-first workflow before modifications.
- User asked this principle to be reflected in agent behavior norms: no blind iterative edits; provide clear status/progress updates before disruptive actions.
- Memory plugin state snapshot:
  - `memory-lightrag` can load but logs warn: `loaded without install/load-path provenance` (treated as untracked local code).
  - Warning is about trust/provenance, not immediate crash.
  - Active memory behavior still appears core-like in CLI outputs; slot/ownership path remains unresolved.
- Communication preference: user wants concise milestone reports and visibility into what is being done, especially during long troubleshooting sequences.
- New durable principle from user (after repeated startup log snippets):
  - Treat `Exec completed (code 0)` + `Registered xxx tool` bursts as likely normal registration first, not automatic incident.
  - Diagnose in layers (process → registration/source conflict → functional verification), then decide action.
  - Always single-variable changes + traceable evidence to avoid repeated pitfall.
- Recorded this into `knowledge/OPENCLAW_BEST_PRACTICES.md` under plugin section: “启动日志判读（避免误踩坑）”.
- New lesson (ACP route failure):
  - Do not assume codex-acp can use any public model endpoint; must run quick smoke test before assigning coding-agent tasks.
  - If smoke shows 404/401 on `/responses`, stop spawn attempts and report concrete base URL + status + root cause.
  - User preference reinforced: avoid choosing an unverified route when alternatives exist; verify first, then execute.
- User reinforced rule (2026-03-23 05:23 UTC): follow official docs first, no speculative local tweaks.
  - Document sources fixed in workflow: `/app/docs/cli/acp.md`, `/app/docs/providers/openai.md`, `/app/extensions/acpx/skills/acp-router/SKILL.md`.
  - Must distinguish OpenClaw model-provider layer from ACP adapter layer when diagnosing compatibility.
