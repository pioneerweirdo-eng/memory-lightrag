- 2026-03-24: 继续推进“飞书高级设置”UI。确认 openclaw-config-fields-full.tsv 已包含 channels.feishu（180 个字段），generated.ts 中存在 channelsSchemaDict 且包含 channels.feishu.*（如 renderMode/streaming/replyInThread/textChunkLimit/tools.* 等）。
- 发现问题根因是 dashboard-ui 的 dev/build 脚本未自动执行 scripts/generate-dict.mjs，导致 generated.ts 可能陈旧从而 UI 看不到 channels.*。已修复：在 apps/dashboard-ui/package.json 中将 dev/build 改为先运行 generate-dict 再启动/构建，并完成一次 build（产物写入 plugins/model-config-admin/dist/ui）。
